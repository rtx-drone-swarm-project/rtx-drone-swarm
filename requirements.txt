# Repo-level Python dependencies for helper scripts and shared tests.
# Backend service dependencies live in backend/requirements.txt.
# SITL container helpers live in sitl/Dockerfile; frontend deps live in frontend/package.json.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pymavlink==2.4.49
pytest>=7.0.0
httpx>=0.27.0,<1.0.0
