# Backend service dependencies used by backend/Dockerfile and local uvicorn runs.
# Repo-level helper script and test dependencies live in ../requirements.txt.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pymavlink==2.4.49
numpy>=1.26.0